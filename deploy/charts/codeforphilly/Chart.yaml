apiVersion: v2
name: codeforphilly
description: CodeForPhilly modernization — Fastify + Vite/React + gitsheets, single replica
type: application
# version: chart SemVer — bumped per Chart change.
version: 0.1.0
# appVersion: bumped per image release; CI overrides with --set image.tag.
appVersion: "0.0.0"
home: https://codeforphilly.org
sources:
  - https://github.com/CodeForPhilly/codeforphilly-rewrite
maintainers:
  - name: Code for Philly
    url: https://codeforphilly.org
